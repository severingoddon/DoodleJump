export enum Visibility {
    VISIBLE,
    INVISIBLE
}

export enum Type {
    NORMAL_PLATFORM,
    BOTTOM_PLATFORM,
    BROKEN_PLATFORM,
    BROKENDESTROYED_PLATFORM,
    LEFTRIGHTMOVING_PLATFORM,
    DISAPPEARING_PLATFORM,
    BOMB,
    COIN,
    TRAMPOLINE,
    ROCKET
}

export enum MovingDirection{
    LEFT,
    RIGHT
}